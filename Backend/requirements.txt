# ============================================================
# AI Research Assistant — requirements.txt
# Fixed: single clean install, pinned versions, Windows 10 compatible
# ============================================================

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Database & Auth
sqlalchemy==2.0.30
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
python-dotenv==1.0.1

# LangChain — pinned to avoid conflicts (FIX: was contradictory installs)
langchain==0.2.16
langchain-community==0.2.16
langchain-core==0.2.38
langchain-text-splitters==0.2.4
langchain-ollama==0.1.3

# Vector store (CPU-only — compatible with 8GB RAM + no GPU requirement)
faiss-cpu==1.8.0

# PDF processing
pypdf==4.3.1

# Document processing (DOCX, XLSX, TXT)
python-docx>=1.1.0
openpyxl>=3.1.0
docx2txt>=0.8

# HTTP client (for Ollama health check)
httpx==0.27.0
